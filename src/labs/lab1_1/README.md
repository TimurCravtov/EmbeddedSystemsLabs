## Util commands for Platform.IO ClI:

- Run and build:

```bash
pio run
```

- Upload to specific target 

```bash
pio run -t upload -e nano
```

- Start serial monitor

```bash
pio device monitor
```